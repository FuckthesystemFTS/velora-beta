# Forum Moderation

The database includes `forum_moderation_actions` for audit records.

Current backend supports message persistence, status fields and moderation audit storage. Full admin moderation UI is still a final-beta blocker before `VELORA_PUBLIC_BETA_READY`.
