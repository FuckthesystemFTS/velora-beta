# macOS Known Limitations

- DMG not generated until GitHub Actions runs successfully.
- App is unsigned and not notarized.
- Manual Mac smoke test has not been performed locally.
- Keychain-specific secure storage still requires runtime verification.

