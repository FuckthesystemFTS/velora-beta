# Troubleshooting

- If `cargo check` is slow on first run, let Tauri dependencies finish compiling.
- If `cargo check` fails on crate download, retry after network recovery.
- If `velora validate` fails, inspect blocked files and manifest errors first.
