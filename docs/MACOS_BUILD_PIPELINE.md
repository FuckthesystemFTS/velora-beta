# macOS Build Pipeline

Workflow: `.github/workflows/build-macos-beta.yml`

Trigger: manual `workflow_dispatch`.

Targets:
- Apple Silicon: `aarch64-apple-darwin` on `macos-15`.
- Intel: `x86_64-apple-darwin` on `macos-15-intel`.

Status: workflow created, execution depends on GitHub remote/authentication.

