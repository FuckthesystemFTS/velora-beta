# macOS Support

Velora uses the same Tauri codebase for Windows and macOS. The beta target is unsigned and non-notarized macOS packages built by GitHub-hosted runners.

No Apple Developer account is required for this beta stage.

